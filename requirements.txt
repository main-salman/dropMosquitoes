# Sniper Messy Mortar — Python Dependencies
# Install: pip install -r requirements.txt
#
# NOTE: On Jetson, install TensorRT via JetPack SDK, not pip.
# NOTE: opencv-python is pre-installed on JetPack OS.
# NOTE: Solenoid gate (BCM 27/PY.00) uses libgpiod v1, installed via apt — NOT pip:
#         sudo apt-get install -y python3-libgpiod
#       (pip 'gpiod' is the incompatible v2 API; do not use it here.)
#       Jetson.GPIO only reaches ~1.6V on this SPI pad; libgpiod drives full 3.3V.

flask>=2.3.0
pyserial>=3.5
smbus2>=0.4.0
numpy>=1.24.0,<2.0.0
ultralytics>=8.0.0

# PCA9685 I2C servo driver (ServoTurretController)
adafruit-blinka>=8.0.0
adafruit-circuitpython-servokit>=1.3.0
adafruit-circuitpython-pca9685>=3.4.0

# Dev/testing only (not needed on Jetson):
# opencv-python>=4.8.0
